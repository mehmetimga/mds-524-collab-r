# mehmetmgaR524

Practice R package. Includes a simple helper `add_two()`.

## Install (local)

```r
install.packages("devtools")
devtools::install_local(".")
```

## Usage

```r
library(mehmetmgaR524)
add_two(1, 2)
```

## Tests

```r
install.packages("testthat")
devtools::test()
```

